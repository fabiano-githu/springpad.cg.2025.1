# Projeto ONCredit - Pequenos Detalhes

*`Por Luferat — 04/02/2026`*

> **VERSIONAMENTO**
> - _Crie um novo branch baseado no anterior._

O "ONCredit" está ficando grande, com mais páginas, o que pode tornar a navegação demorada.
Por exemplo, se estamos na página do "Histórico do cliente", precisamos clicar em `←` várias vezes até voltar à "Home".

Para lelhorar um pouco isso, vamos deixar o logotipo clicável e levando à "Home".

Edite `lib/templates/apptitle.dart` para o seguinte:

|[`lib/templates/apptitle.dart`](https://raw.githubusercontent.com/Luferat/oncredit/refs/heads/006.Pequeno_detalhe/lib/templates/apptitle.dart)|
|---|
| _Mantenha <kbd>Ctrl</kbd> pressionado ao clicar no link._ |

Teste o funcionamento do aplicativo e da navegação.

## Página de Configurações

Jé temos um link com engrenagem no cabeçalho, mas ele está inerte, sem funcionalidade.
Vamos melhorar isso, criando a página de "Configurações" que vamos incrementar no futuro.

1. Atualize `lib/main.dart` para adicionar a rota `/settings` em `routes`:

|[`lib/main.dart`](https://raw.githubusercontent.com/Luferat/oncredit/refs/heads/006.Pequeno_detalhe/lib/main.dart)|
|---|
| _Mantenha <kbd>Ctrl</kbd> pressionado ao clicar no link._ |

2. Crie a página `lib/pages/settings.dart` com o conteúdo abaixo:

|[`lib/pages/settings.dart`](https://raw.githubusercontent.com/Luferat/oncredit/refs/heads/006.Pequeno_detalhe/lib/pages/settings.dart)|
|---|
| _Mantenha <kbd>Ctrl</kbd> pressionado ao clicar no link._ |

Teste mais uma vez, se tudo funciona, inclusive a "engrenagem"!

Agora, é o melhor momento para garantir que tudo que foi implementado, funciona, testando o aplicativo nos dispositivos disponíveis.

_Também, se quiser, pode fazer o `merge` do branch `main` com o mais recente, deixando o versionamento em dia!_

---

<p align="center"><a href="#file-00-md">↑ Sumário</a></p>
